{
  "devDependencies": {
    "vitepress": "^1.5.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev --force",
    "docs:build": "vitepress build --force",
    "docs:preview": "vitepress build --force && vitepress preview"
  }
}